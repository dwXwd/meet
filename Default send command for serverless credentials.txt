{
  "CLIENT_ID": "YOUR_CLIENT_ID",
  "PROJECT_ID": "YOUR_PROJECT_ID",
  "CLIENT_SECRET": "YOUR_CLIENT_SECRET",
  "CALENDAR_ID": "fullstackwebdev@careerfoundry.com"
}

serverless config credentials --provider aws --key AKIAS6WA2ZUJA6L4UQR7 --secret wWBxmafq85fTusQL4REtzmLTnyUwMPraZxlmXKML


TO add in serverless.yms

service: auth-server

provider:
  name: aws
  runtime: nodejs12.x
  region: eu-central-1
  timeout: 60 # optional, in seconds, default is 6
  environment:
    CLIENT_ID: ${file(config.json):CLIENT_ID}
    CLIENT_SECRET: ${file(config.json):CLIENT_SECRET}
    PROJECT_ID: ${file(config.json):PROJECT_ID}
    CALENDAR_ID: ${file(config.json):CALENDAR_ID}
functions:
  getAuthURL:
    handler: handler.getAuthURL
    events:
      - http:
          path: api/get-auth-url
          method: get